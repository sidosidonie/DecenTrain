#!/usr/bin/env python3


# Write a simple FFN Layer for multi machine to profile the performance

import logging
import torch


class MLPVerifyMultiNode:

    def __init__(self, origin_mlp: Mlp):

        print(node)
